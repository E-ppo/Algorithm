//* 두수의 나눗셈

const solution = (num1, num2) => Math.floor((num1 / num2) * 1000);

// const solution = (num1, num2) => Math.trunc((num1 / num2) * 1000);
// Math.trunc() 메서드는 소수 자릿수를 제거하여 숫자의 정수 부분을 반환함
